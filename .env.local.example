# Environment variables for local development.
# Copy this file to .env.local and fill in the required values.                                                                                                                                                                                 
# This value is read by the dev/prod proxy and is NOT exposed to the browser bundle.
HF_TOKEN=your_hf_token_here

# Optional overrides
# VoiceConsultant text model (HF Inference text-generation)                                                                                                                                                                                                                                                                                                                                 
VITE_HF_TEXT_MODEL=Qwen/Qwen2.5-7B-Instruct

# Style extractor VLM (HF Inference Chat Completions)
VITE_HF_VLM_MODEL=Qwen/Qwen2.5-VL-7B-Instruct

# Style embedding model (HF Inference feature-extraction)
VITE_HF_EMBED_MODEL=openai/clip-vit-base-patch32

# Flux image model (HF Inference text-to-image)
# Defaulting to a widely available open model to avoid gating issues. Override if you have access to a preferred model.
VITE_HF_FLUX_MODEL=runwayml/stable-diffusion-v1-5
